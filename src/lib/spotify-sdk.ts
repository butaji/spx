/**
 * Spotify SDK - Re-export from spotify.ts for backward compatibility.
 * Tests mock this module, so we keep it as an alias.
 */

// Re-export everything from spotify.ts
export * from './spotify';
